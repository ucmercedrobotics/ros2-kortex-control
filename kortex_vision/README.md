# Detect Object (YOLO)
In order to use this node, you must have a Kinova Kortex with the vision bracelet.
Additionally, you must be running `make vision` while connected to real hardware for this to work.
